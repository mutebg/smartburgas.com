import { h } from 'preact';
import { Link } from 'preact-router/match';
import './style.scss';

const Header = () => (
	<header className="header">
		<h1>www.burgas.app</h1>
	</header>
);

export default Header;
